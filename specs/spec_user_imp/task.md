# 实施计划

- [x] [1]. 批量确认与题号黄金匹配后端  
— 具体要做的事情：新增 `confirm_all`、`bulk_confirm` 接口；接入大模型返回的 `normalizedLabel`；实现字符占比回退匹配并将策略写入 `matchStrategy`；扩展 `Response.extra_metadata` 以保存 `suspiciousMatches`、`blockedSupplement`。  
— 需求：1,2,3

- [x] [2]. Qwen 提示词与结果校验调优  
— 具体要做的事情：分别为严格匹配、智能参考构建中文提示词模板；要求模型输出 `subQuestions.normalizedLabel`、匹配度与可疑理由；在 strict 模式下仅允许 `blockedSupplement` 中文说明；实现响应校验与落库逻辑。  
— 需求：2,3

- [x] [3]. 标准答案校对前端重构  
— 具体要做的事情：在 `StepAnswerReview` 实现中文批量按钮、子问视图、答案模式切换，以及与阻断提示联动的进度条；确保所有文案与提示使用中文。  
— 需求：1,2,3

- [x] [4]. AI 批改确认前端优化  
— 具体要做的事情：在 `StepReviewConfirm` 实现按筛选批量操作、可疑匹配徽标、撤销交互与操作历史抽屉入口，默认视图保持简洁中文展示。  
— 需求：1,3,4

- [x] [5]. 向导守卫与导出动线升级  
— 具体要做的事情：扩展 `WizardProvider` 的阻断状态机；重构完成页三栏卡片与中文 CTA；引入渐进披露的辅助信息与「操作历史」悬浮按钮；修复导出按钮激活逻辑。  
— 需求：1,4

- [x] [6]. 测试与验收  
— 具体要做的事情：补充后端单测、前端集成测试与手动回归用例，覆盖复合题号、两种答案模式、导出流程；检查所有页面文案为中文无英文残留。  
— 需求：1,2,3,4
